#include <list>
#include <fstream>
#include <iostream>
#include <vector>


std::list<int> parse_input(void);
std::vector<int> parse_vector(void);
unsigned part_one(std::list<int> numbers);
unsigned part_two(std::vector<int> numbers);