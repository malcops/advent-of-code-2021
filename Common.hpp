#include <fstream>
#include <iostream>
#include <string>
#include <sstream>
#include <tuple>
#include <vector>

typedef std::vector<std::string> inputVector;
inputVector parse_input(std::string inputFile);